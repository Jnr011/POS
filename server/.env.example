# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration (for legacy auth endpoints)
JWT_SECRET=your_jwt_secret_key_here

# Sync Relay
SYNC_RELAY_URL=http://localhost:5000/api/sync

# Cloud Sync (optional — for multi-store relay)
CLOUD_SYNC_URL=
CLOUD_API_KEY=
